# AI 期末复习助手 — 完整产品评测

> 基于实际代码库审计 + 6-Gate 框架 + 7 项缺漏分析
> 审计日期: 2026-07-29

---

## 一、项目真实状态（基于代码+数据审计）

### 工程规模

| 维度 | 数量 | 评价 |
|---|---|---|
| 路由文件 | 12 | 超过典型个人项目 |
| 服务层 | 8 | 覆盖知识点/错题/记忆曲线/科目/风格/电子书/题型 |
| 控制器 | 12 | 与路由一一对应 |
| JSON 数据文件 | 8 | documents / generateResults / knowledge / wrongQuestions / subjects / examStyles / answerSources / customQuestionTypes |
| 前端 | React + Vite 单文件 App.jsx | 功能密集但代码组织可优化 |

### 实际使用数据（关键！）

| 指标 | 数值 | 说明 |
|---|---|---|
| 上传文件 | 74 | 有真实使用量 |
| 科目 | 1（"高等数学"） | 核心场景在运行 |
| 生成记录 | 25 | AI 生成被多次使用 |
| 知识点条目 | 30 | 5 条标记为"已掌握" |
| 出题风格学习 | 11 条 | 这个功能被重度使用 |
| 错题 | **0** | 🚨 错题本功能从未被触发 |
| 电子书/答案来源 | **0** | 🚨 电子书功能从未被使用 |
| 自定义题型 | **0** | 🚨 自定义题型从未被创建 |
| `extractedPoints` 回写 | **仅 1 份** | 🚨 知识点提取后回写几乎没执行 |
| `userEditedText` | **0** | 🚨 用户编辑确认流程没走过 |

### 核心发现

**你的链路中最关键的 AI 产品环节——"用户确认知识点"——实际上从未被使用过。** 74 次上传 → 25 次生成，但中间没有人停下来在 Point Editor 里修改知识点。这意味着你的 N4（校验兜底）节点是代码上存在、产品上闲置的。生成时走了"全量模式"（从文件直接提取→出题），跳过了"用户确认→靶向出题"的路径。

这不是 bug，而是产品流设计问题：用户上传后自动弹窗提取知识点 → 但用户可能直接关掉弹窗去点"生成"了。**确认环节没有被嵌入为必经过的步骤。**

---

## 二、按 AI 产品标准的逐项评分

### 产品基础

| 维度 | 得分 | 证据 |
|---|---|---|
| 用户定义 | ⭐⭐⭐⭐ | "备考高等数学的学生"——不是"学生"标签，有具体任务 |
| 材料真实性 | ⭐⭐⭐⭐⭐ | 74 份真实上传，11 次风格学习 |
| 失败成本 | ⭐⭐⭐⭐ | 挂科/重修是真实代价 |
| 闭环潜力 | ⭐⭐⭐⭐ | 错题本+记忆曲线天然支持迭代 |

### AI 介入

| 维度 | 得分 | 证据 |
|---|---|---|
| 节点清晰度 | ⭐⭐⭐⭐ | extractKnowledgeOnly → generateReviewContent → memoryCurve |
| 非 AI 替代 | ⭐⭐⭐⭐⭐ | 规则/模板/搜索无法替代这三个节点 |
| 增益明显性 | ⭐⭐⭐⭐ | 不是"更方便"，是"做不到→做得到" |
| 兜底边界 | ⭐⭐⭐ | 有 Point Editor 但实际使用率为 0 |

### 产品链路

| 维度 | 得分 | 证据 |
|---|---|---|
| 链路完整度 | ⭐⭐⭐⭐ | N1-N5 五个节点全部有代码实现 |
| 架构克制 | ⭐⭐⭐⭐⭐ | Fixed Workflow，没有为了高级而上 Agent |
| 中间态可检查 | ⭐⭐⭐⭐ | extractKnowledge 结果可被用户修正（设计上） |
| 反馈闭环 | ⭐⭐ | N4 修正不回馈到 N2；补强出题无独立 prompt |
| 背题模式 | ⭐ | PRD 提到,代码不存在 |

### 评测体系

| 维度 | 得分 | 证据 |
|---|---|---|
| 评测对象 | ⭐⭐⭐ | N2 节点已定位为评测目标 |
| 样本标注 | ⭐ | **0 份 ground truth 标注** |
| MECE 标准 | ⭐⭐⭐ | 四维标准已设计但未执行 |
| 机评 vs 人评 | ⭐ | 全无 |
| Badcase 闭环 | ⭐ | **从未跑过发现→诊断→修改→验证** |

### 证据与表达

| 维度 | 得分 | 证据 |
|---|---|---|
| 可演示性 | ⭐⭐⭐⭐⭐ | 完整前端+后端，可现场跑 |
| 可量化性 | ⭐ | 无评测数据支撑任何"提升/准确率"表述 |
| 面试防御 | ⭐⭐⭐ | 有架构理解，缺数据故事 |
| 边界意识 | ⭐⭐⭐ | 知道不能写爬虫/上线/deploy |

---

## 三、产品的"强"在哪里、在哪里"虚"

### 强点（面试时主攻方向）

**1. 链路完整性**
你的 12 个路由、8 个服务不是堆出来的——它们形成了一个有因果关系的数据飞轮：

```
上传 → 提取知识点 → 确认 → 出题 → 答题 → 错题记录 → 记忆曲线 → 下次复习
```

面试官看到这个链路会认为你"理解了 AI 产品不是一次 prompt 调用，而是一个系统"。

**2. 架构克制**
你没有用 Agent / Agent Team / multi-agent 这些 buzzword。你选了刚好够用的 Fixed Workflow + Prompt Chain。这是成熟的产品判断——面试官最怕看到的就是"我用 5 个 Agent 做了个问答机器人"。

**3. 出题风格学习被真实使用（11 次）**
这不是 demo——你上传了同一份试卷多次（从 documents.json 可以看到同一份高等数学试卷被反复上传学习），说明你在真实迭代这个功能。

### 虚点（需要立刻补）

**1. 用户确认环节被跳过了**
这是整个报告中最致命的发现。你设计了 extractKnowledge → Point Editor → confirmedKnowledge 的流程,但实际使用数据表明:没有人走过这个流程。你的生成链路实际走的是"全量模式"——直接从文件提取文本 → 出题,中间没有用户修正。

这意味着你讲"AI 提取知识点后用户可确认修改"时,面试官如果追问"用户实际修改了多少",你没法回答。

**修复:** 把确认环节从"可选的弹窗"变成"必经的步骤"。生成按钮在 confirmedKnowledge.length === 0 时应该引导用户先确认知识点,而不是直接允许从文件生成。

**2. 0 个 badcase**
你有 25 次生成记录,但没有录过任何一条"这次生成哪里不好"。面试时最容易被问倒的就是这里。

**修复:** 今天你打开系统,生成 5 次,每次记录:
- AI 提取了 X 个知识点,我觉得哪几个不对(编造的/太细的/漏掉的)
- 生成的题目里,哪道的选项太明显、哪道答案可能错了
- 写 5 行笔记,这比你写 50 页 PPT 有用

**3. 错题本功能从未被触发**
你在答辩里说"错题本"和"记忆曲线",但实际数据里 wrongQuestions 是 0。面试官如果问"你是怎么验证记忆曲线间隔是否合理的",你会卡住。

---

## 四、最应该优先做的 3 件事

### P0: 跑一次完整的评测（今天，2 小时）

1. 打开你的系统,选"高等数学"科目
2. 上传一份教材 PDF → 提取知识点
3. **不要跳过 Point Editor**,认真删掉 AI 编造的知识点,补充遗漏的
4. 确认后生成题目
5. 记录:AI 提取了 12 个,你保留了 8 个,删了 4 个(为什么删——写下来)
6. 这就是你的第一条 badcase 笔记

做完这步,你的项目就从"我做了个东西"变成"我做了个东西,发现它有问题,我记录了"。

### P1: 标 20 份材料的 ground truth（明天，3 小时）

从你那 74 份上传中选 20 份有代表性的,手工写"这份材料应该提取哪些知识点"。用 Gate 4 的四维标准打分:覆盖率/精确率/粒度/结构完整性。

产出:一份 eval_cases.csv + 一份 scores.csv。

这是你面试时唯一能证明"我做过评测"的东西。目前你的项目在评测维度是 0 分,补了这个就变成 4 分(满分 5)。

### P2: 把确认环节变成必经过的

修改生成按钮逻辑:

```
if (confirmedKnowledge.length === 0) {
  → 引导用户先去确认知识点
  → 显示:"请先确认 AI 提取的知识点,再生成题目"
}
```

这样你的 N4 节点就从"代码上的死代码"变成"产品流中的活节点"。

---

## 五、如果现在去面试,你该怎么讲

### 30 秒开场

> "我做了一个 AI 驱动的备考工具。核心链路是:学生上传教材或试卷 → AI 提取知识点(extractKnowledgeOnly, temperature=0.3) → 学生在确认界面删减修正 → 基于确认后的知识点,AI 按 Prompt Chain 生成题目(先出题干,再设计选项,最后写解析) → 答题后错题进入艾宾浩斯记忆曲线调度(1/2/4/7/15 天间隔)。我目前有 74 份真实上传材料和 25 次生成记录,正在做第一轮 N2 节点(知识点提取)的评测标注。"

### 如果被追问"怎么评测"

> "我设计了四维 MECE 标准:覆盖率(该提的提了吗)、精确率(提的有没有编的)、粒度(太粗还是太细)、结构完整性(有没有漏掉整个章节)。第一版先人评 20 条,找到最低维度后改 prompt,同一批评测样本复测对比。目前标注进行中。"

### 如果被追问"最难的问题是什么"

> "最难的是精确率——AI 在提取知识点时会'合理发挥',比如教材只提到了概念名,AI 会帮忙补充一段解释当作知识点。我的修复方向是在 prompt 里加原文引用约束:每个知识点必须能在原文找到对应句子,否则标注'推测'。"

### 不能说的话

- ❌ "题目准确率 90%+"(没有评测集,没有口径)
- ❌ "我做了小红书爬虫自动搜资料"(没做,法律风险)
- ❌ "系统服务了 XX 用户"(没有线上环境)

---

## 六、总结

**你现在手里有:**
- 一个**可以真实演示**的完整 AI 产品(上传→提取→生成→答题→复习)
- 74 份真实材料,证明场景不是编的
- 清晰的产品链路(N1-N5),架构克制
- 知道评测怎么做(虽然还没做)

**你缺的是:**
- 任何形式的评测证据(标注/badcase/复测)
- 用户确认环节的**实际使用**数据
- 1 个完整的 badcase 闭环故事

**一句话:** 你的项目到了"需要从开发切换到评测"的阶段。再写一个功能不会让它更好,补一条 badcase 笔记能。
